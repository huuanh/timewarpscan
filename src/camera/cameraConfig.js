// Camera & effect configuration
// These values can be exposed to the user as settings in the future.

const CAMERA_CONFIG = {
    // TimeWarp Scan (waterfall effect)
    waterfallScanDurationMs: 15000, // total scan sweep time in milliseconds
};

export default CAMERA_CONFIG;
