# Copilot Recipes

## Create Android Native Module

Steps:
1 create Kotlin class
2 extend ReactContextBaseJavaModule
3 add ReactMethod
4 register module in Package

## Create React Native Hook

Pattern:

export const useCamera = () => {
}